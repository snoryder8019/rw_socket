ggl redirects for auth remove config.url from callback google
pkg json nodemon dev cmd
base images branding poi's and icon folder preset
add partials/tickets.ejs
add tickets route to routes/index endpoint
remove nanacy name from tickets and card from ticket option
mongo db needs an auto deploy before running the app first time
Move to c .bat completed would be nie
tying it all together in one script then
socket io setup in bin, dependencies and views