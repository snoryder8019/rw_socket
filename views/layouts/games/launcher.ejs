<style>
    h1{
        color: black;
    }
</style>

<%if(typeof data =='object'&& data){%>
    <%data.forEach(data =>{%>
        <h1><%=data.name%></h1><button>join a session</button>
   <%})%>
    <%}%>